<local:DynamicSplashScreen x:Class="RepositoryParser.Controls.SplashScreen.SplashScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RepositoryParser.Controls.SplashScreen"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="300"
             Width="500"
             Height="300"
             x:Name="Root">
    <Window.Resources>

    </Window.Resources>
    <Border BorderThickness="3" BorderBrush="#1a472a">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image Source="ApplicationSplashScreen.png" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
<!--            <TextBlock Grid.Row="1" Text="{Binding ElementName=Root, Path=Message}" FontWeight="Light"/>-->
            <!--<ProgressBar IsIndeterminate="True" Grid.Row="1" Padding="5,0"/>
            <TextBlock Grid.Row="2" Text="{Binding ElementName=Root,Path=Message}" Margin="5" Padding="5,0"/>-->
        </Grid>
    </Border>
</local:DynamicSplashScreen>
